// Re-export the shared ProductCard so customer-scoped pages can
// import it from `../../components/customer/ProductCard` while the
// actual implementation stays in `../../components/ProductCard`.
export { default } from "../ProductCard";
